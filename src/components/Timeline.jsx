import React from 'react';
import SectionTitle from './ui/SectionTitle';

const Timeline = () => {
  return (
    <section id="timeline" className="section-container relative z-10">
      <SectionTitle 
        badge="Event Schedule"
        title="TIMELINE & MILESTONES"
        subtitle="Follow the step-by-step roadmap from registration to grand finale."
      />
    </section>
  );
};

export default Timeline;
